# Phân hệ Frontend (CineSky)
Dự án được xây dựng bằng React và Vite.

## Cách chạy cục bộ (Local Setup)
1. Cài đặt thư viện: `npm install`
2. Khởi chạy server: `npm run dev`
3. Truy cập vào đường dẫn: `http://localhost:5173`