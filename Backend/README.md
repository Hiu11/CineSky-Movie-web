# Phân hệ Backend (CineSky)
Hệ thống API xây dựng bằng Node.js, Express và MongoDB.

## Cách chạy cục bộ (Local Setup)
1. Cài đặt thư viện: `npm install`
2. Cấu hình biến môi trường trong file `.env`
3. Khởi chạy server: `npm run dev`